# Public NPM registry configuration
# This SDK is published to the public NPM registry
# No authentication required for installation

# Ensure public registry is used for all packages
registry=https://registry.npmjs.org/

# Ensure @dufourpascal scope uses public registry
@dufourpascal:registry=https://registry.npmjs.org/